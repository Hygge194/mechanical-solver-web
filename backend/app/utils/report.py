# Để sử dụng, cần thiết phải cài package python-docx: `pip install python-docx`
# from docx import Document

def generate_report(results: dict, filename: str = "Thuyet_Minh_Do_An.docx"):
    """
    Hàm mẫu để xuất file Word thuyết minh tự động.
    """
    # doc = Document()
    # doc.add_heading('THUYẾT MINH ĐỒ ÁN CHI TIẾT MÁY', 0)
    
    # doc.add_heading('Phần 1: Chọn Động Cơ', level=1)
    # doc.add_paragraph(f"Công suất yêu cầu: {results.get('power')} kW")
    
    # # Lưu file
    # file_path = f"/tmp/{filename}"
    # doc.save(file_path)
    # return file_path
    
    pass
